version: '3.8'

services:
  # Service pour isoler Payload CMS sur le port 3300
  payload-cms:
    build:
      context: .
      dockerfile: Dockerfile.payload
    ports:
      - '3300:3000'
    environment:
      - MONGODB_URI=mongodb+srv://aurelien_db_user:UabCxoHI9J4C75j0@clustergestionmaxformat.a9qrz87.mongodb.net/gestionmax-formation?retryWrites=true&w=majority&appName=Clustergestionmaxformation
      - PAYLOAD_SECRET=your-secret-key-change-this-in-production-gestionmax-2024
      - NEXT_PUBLIC_SERVER_URL=http://localhost:3300
      - NEXTAUTH_URL=http://localhost:3300
      - NEXTAUTH_SECRET=your-nextauth-secret-change-this-in-production-gestionmax-2024
    volumes:
      - ./media:/app/media
      - ./src:/app/src
      - ./public:/app/public
      - ./next.config.ts:/app/next.config.ts
      - ./tailwind.config.ts:/app/tailwind.config.ts
      - ./tsconfig.json:/app/tsconfig.json
      - ./postcss.config.mjs:/app/postcss.config.mjs
    networks:
      - payload-network
    command: npm run dev

networks:
  payload-network:
    driver: bridge
